<form method="post" action="{{ path('pro_info_delete', {'id': pro_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pro_info.id) }}">
    <button class="btn">Delete</button>
</form>
