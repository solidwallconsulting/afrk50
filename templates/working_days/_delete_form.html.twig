<form method="post" action="{{ path('working_days_delete', {'id': working_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ working_day.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
