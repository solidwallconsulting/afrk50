<form method="post" action="{{ path('payments_plans_delete', {'id': payments_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payments_plan.id) }}">
    <button class="btn">Delete</button>
</form>
