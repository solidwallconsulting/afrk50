{% extends 'base.html.twig' %}

{% block title %}PaymentsPlans{% endblock %}

{% block body %}
    <h1>PaymentsPlans</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payments_plan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ payments_plan.name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ payments_plan.duration }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ payments_plan.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payments_plans_index') }}">back to list</a>

    <a href="{{ path('payments_plans_edit', {'id': payments_plan.id}) }}">edit</a>

    {{ include('payments_plans/_delete_form.html.twig') }}
{% endblock %}
