 
{% extends 'admin-base.html.twig' %}


{% block body %}


	<main class="main-content">
		{% block topNav %}
			{{ parent() }}
		{% endblock %}

		<div class="conatiner-fluid content-inner mt-n5 py-0">
			<div class="row">

				<div class="col-12">
					<div class="card">
						<div class="card-header d-flex justify-content-between flex-wrap">
							<div class="header-title">
								<h4 class="card-title">Payments plans</h4>
							</div>

							<div>

								<!--<a class="btn btn-primary btn-sm" href="{{ path('payments_plans_new') }}">Add</a>-->
							</div>

						</div>
						<div class="card-body w-100">


							 <table class="table datatable">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Name</th>
                                        <th>Duration</th>
                                        <th>Price</th>
                                        <th>actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for item in payments_plans %}
                                    <tr>
                                        <td>{{ loop.index}}</td>
                                        <td>{{ item.name }}</td>
                                        <td> <span class="badge bg-info text-white" >{{ item.duration }} days</span> </td>
                                        
                                        <td>{{ item.price |format_currency('EUR') }}</td>
                                        
                                        <td>
                                        <a href="{{ path('payments_plans_edit', {'id': item.id}) }}">edit</a> 
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="4">no records found</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

						</div>


					</div>

				</div>


			{% endblock %}
