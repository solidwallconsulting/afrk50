{% extends 'base.html.twig' %}

{% block title %}ListingAttributes{% endblock %}

{% block body %}
    <h1>ListingAttributes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ listing_attribute.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('listing_attributes_index') }}">back to list</a>

    <a href="{{ path('listing_attributes_edit', {'id': listing_attribute.id}) }}">edit</a>

    {{ include('listing_attributes/_delete_form.html.twig') }}
{% endblock %}
