<form method="post" action="{{ path('listing_attributes_delete', {'id': listing_attribute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listing_attribute.id) }}">
    <button class="btn">Delete</button>
</form>
