 

{% extends 'admin-base.html.twig' %}


{% block body %}


	<main class="main-content">
		{% block topNav %}
			{{ parent() }}
		{% endblock %}

		<div class="conatiner-fluid content-inner mt-n5 py-0">
			<div class="row">

				<div class="col-12">
					<div class="card">
						<div class="card-header d-flex justify-content-between flex-wrap">
							<div class="header-title">
								<h4 class="card-title">Partners</h4>
							</div>

                            <a href="{{ path('partners_new') }}" class="btn btn-primary">Add</a>


						</div>
						<div class="card-body w-100">

<table class="table">
		<thead>
			<tr>
				<th>#</th>
				<th>Name</th>
				<th>Logo</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for partner in partners %}
				<tr>
					<td>{{ partner.id }}</td>
					<td>{{ partner.name }}</td>
					<td> <img src="{{ partner.logoURL }}" style="width:200px" class="rounded" /> </td>
					<td> 
						<a href="{{ path('partners_edit', {'id': partner.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="4">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
							 

						</div>


					</div>

				</div>


			{% endblock %}
