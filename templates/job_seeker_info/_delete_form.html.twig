<form method="post" action="{{ path('job_seeker_info_delete', {'id': job_seeker_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_seeker_info.id) }}">
    <button class="btn">Delete</button>
</form>
