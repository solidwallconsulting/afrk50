{% extends 'base.html.twig' %}

{% block title %}Promotions{% endblock %}

{% block body %}
    <h1>Promotions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ promotion.duration }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ promotion.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('promotions_index') }}">back to list</a>

    <a href="{{ path('promotions_edit', {'id': promotion.id}) }}">edit</a>

    {{ include('promotions/_delete_form.html.twig') }}
{% endblock %}
