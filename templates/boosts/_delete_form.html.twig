<form method="post" action="{{ path('promotions_delete', {'id': boost.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boost.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
